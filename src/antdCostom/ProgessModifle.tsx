import { Progress, Button } from "antd";
import styled from "styled-components";

export const ProgessModiflie = styled(Progress)`
  .ant-progress-text {
    color: white;
  }
`;

export const StyledButton = styled(Button)`
  color: green;
  width: 400px;
`;
